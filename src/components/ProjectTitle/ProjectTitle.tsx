import styles from './ProjectTitle.module.scss';

export default function ProjectTitle() {
  return (
    <div className={styles.container}>
      <span className={styles.title}>Строительно-монтажные работы</span>
    </div>
  );
}
